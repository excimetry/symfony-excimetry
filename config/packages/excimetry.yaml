# This file is the excimetry bundle configuration template
# It will be copied to config/packages/excimetry.yaml during installation

excimetry:
    # Whether the bundle is enabled
    enabled: true
    period: 0.01
    mode: wal